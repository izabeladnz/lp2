O propósito da palavra-chave new é instanciar um objeto. Ele reserva memória para o objeto e cria uma referência à ele. Precisamos usar o método construtor que será usado como base para instanciar a classe e gerar o objeto.
